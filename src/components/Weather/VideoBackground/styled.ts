import styled from '@emotion/styled'

const Video = styled.video`
  min-height: 100vh;
  width: 100vw;
  object-fit: cover;
  position: fixed;
  top: 0;
  left: 0;
  background: #ccc;
`

export { Video }
